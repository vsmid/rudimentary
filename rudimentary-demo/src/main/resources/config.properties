# Server
server.port=8888
server.threadPoolSize=25
server.stopDelay=0

# MVC 
mvc.templatesDir=view
mvc.staticResourcesDir=static

# Security
security.realm=demo
security.urisRequiringAuthentication=health,userInfoEndpoint
security.basic.enabled=true
security.identityStore.embedded.identities=\
vsmid:pass:admins::email=vsmid@gmail.com,city=Zagreb;\
mmeglic:pass::read,write;\

# Security - CSRF
security.csrf.enabled=false
security.csrf.stateless=false

# Security - CORS
security.cors.enabled=true

# Session
session.create=true
session.inactivityPeriodAllowed=0

# JDBC Datasource
dataSource.driverClassName=org.sqlite.JDBC
dataSource.jdbcUrl=jdbc:sqlite::memory:?cache=shared
dataSource.maximumPoolSize=25
dataSource.username=
dataSource.password=